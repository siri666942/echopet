# EchoPet 前端黑客松开发流程

更新时间：2026-05-30

## 适用前提

这份流程基于你当前目录里的两套现成项目与两份可读文档：

- 桌宠前端基座：`DyberPet`
- 环境感知数据源：`ActivityWatch`
- 语音识别方案：`faster-whisper`
- 播放执行层：`mpv`
- 产品文档：`product_design.md`
- 技术文档：`techneque_design.md`

同时结合你现在的角色定位：

- 你负责前端开发
- 这是黑客松项目
- 目标不是做“大而全架构”，而是做一个 `能演示、能打动评委、能顺利联调` 的 MVP

---

## 先给结论

### 这次黑客松前端怎么做最稳

**不要再额外起一个新的 Electron/React 桌面前端。**

更推荐的路线是：

- 直接把 `DyberPet` 当作前端表现层和桌宠基座
- `ActivityWatch` 单独运行，作为环境感知数据源
- `faster-whisper` 负责把用户输入音频识别成文本
- `mpv` 负责歌曲播放与切换
- 你们自己的后端模块负责状态判断、记忆、编排和音乐决策
- 前端只做：
  - 桌宠形象
  - 状态变化
  - 输入入口
  - 反馈展示
  - 与后端的最小联调

原因：

- `DyberPet` 已经解决了桌宠最难的部分：透明窗体、桌面驻留、动作资源组织、气泡、托盘、基础系统面板
- `ActivityWatch` 已经解决了环境感知的主要数据采集
- `faster-whisper` 适合黑客松里快速做出“说一句话就能理解”的能力
- `mpv` 适合做可控、稳定、可脚本化的本地播放执行层
- 黑客松时间有限，**最不划算** 的事情就是一边做新前端壳子，一边再接桌宠逻辑

所以，这次你这个“前端”更准确地说，是：

**桌宠表现层负责人 + 交互层负责人 + 联调入口负责人**

---

## 整体架构建议

这次最推荐的整体形态不是“一个大程序包打一切”，而是：

`DyberPet` 做桌宠基座  
`Agent Backend` 做大脑  
`ActivityWatch + faster-whisper + mpv` 做能力模块

### 推荐分层

```text
DyberPet 基座层
  ├─ 桌宠形象与状态动画
  ├─ 气泡 / 推荐反馈 / 托盘
  ├─ 文本输入窗
  └─ 录音触发入口

前端适配层
  ├─ agent_client
  ├─ state_mapper
  ├─ whisper_client
  └─ player_status_adapter

Agent Backend
  ├─ context_collector
  ├─ memory_store
  ├─ llm_orchestrator
  ├─ music_decider
  └─ player_controller

外部能力
  ├─ ActivityWatch
  ├─ faster-whisper
  └─ mpv
```

### 分工解释

- `DyberPet` 负责“看得见的部分”
- `Agent Backend` 负责“想得明白的部分”
- `ActivityWatch` 提供环境信号
- `faster-whisper` 提供输入转文本
- `mpv` 提供最后的播放执行

### 为什么 `DyberPet` 适合做基座

- 它本来就是桌宠 UI 工程，不需要你重新搭透明窗口、托盘、动作系统
- 它是 `Python`，和后端记忆、状态机、LLM 编排天然同栈
- 它已经有气泡、动作、通知这些很适合承接 Agent 输出的 UI 位

### 但它不应该做什么

- 不应该直接承载完整记忆系统
- 不应该直接承担复杂编排逻辑
- 不应该把所有状态判断都写在 `DyberPet.py` 里

一句话：

**`DyberPet` 适合做基座，但不适合做大脑。**

---

## 你负责的前端范围

基于 `techneque_design.md` 的描述，理想上前端负责：

- 桌宠 UI
- 录音入口
- 状态展示
- 音乐播放反馈
- 用户反馈

但结合这次黑客松现状，我建议你把范围收敛为下面 5 个模块：

### 模块 1：桌宠角色与视觉

包括：

- 角色主形象
- 状态图
- 图标
- 托盘图标
- 气泡风格
- 统一色彩和文案语气

### 模块 2：状态表现

把后端状态映射成桌宠可见反馈：

- `focus`
- `tired`
- `frustrated`
- `sad`
- `idle`

每个状态至少有：

- 一张主图或一组简单帧动画
- 一条气泡文案
- 一个推荐反馈样式

### 模块 3：交互入口

至少做一个用户输入入口，并且本次建议直接定为：

- 文本输入窗
- 录音按钮
- `faster-whisper` 识别结果回填到文本

也就是说，用户体验是：

- 用户点击输入入口
- 直接打字
- 或点击录音
- 录音结果先经过 `faster-whisper`
- 前端拿到识别文本后，再把文本发给后端

这样做的好处是：

- 前端主链路仍然是“文本输入”
- 语音只负责把音频转成文本
- 后端接口始终统一收 `text`

不建议做成：

- 前端直接把音频原文件一路传给大模型
- 识别、理解、推荐三件事全部耦在一个调用里

### 模块 4：推荐反馈展示

需要把“系统做了什么”显式展示出来。

例如：

- “检测到你正在深夜写代码”
- “我猜你需要更专注一点的音乐”
- “已为你播放 LoFi Focus”

### 模块 5：联调壳层

你需要准备一个最小前端接口层，让桌宠能接收后端结果：

- 当前状态
- 推荐结果
- 播放状态
- 用户反馈结果

---

## 基座与模块实施思路

如果你们按这套方案推进，建议把开发实施拆成下面 5 层。

### 第 1 层：`DyberPet` 基座层

负责：

- 桌宠角色资源
- 状态动画
- 气泡
- 托盘
- 桌宠输入入口
- 推荐反馈显示

这一层只关心“怎么展示”。

### 第 2 层：前端适配层

建议新增你们自己的几个轻量模块：

```text
frontend/
  agent_client.py
  state_mapper.py
  input_panel.py
  whisper_adapter.py
  playback_panel.py
```

它们分别负责：

- `agent_client.py`
  - 请求后端 API
  - 获取状态、推荐、回复
- `state_mapper.py`
  - 把后端返回的 `current_state` 映射到桌宠动作、气泡、UI 文案
- `input_panel.py`
  - 负责文本输入框和录音按钮
- `whisper_adapter.py`
  - 负责调用 `faster-whisper`，把音频转成文本
- `playback_panel.py`
  - 展示当前推荐歌曲和播放状态

### 第 3 层：环境与输入采集层

由后端或独立服务负责：

- 拉取 `ActivityWatch`
- 读取时间、活跃窗口、AFK
- 接收前端文本
- 接收 `faster-whisper` 结果

### 第 4 层：Agent 编排层

由后端负责：

- 把“用户说的话 + 当前环境 + 历史记忆”组合起来
- 输出：
  - 当前状态
  - 推荐理由
  - 推荐歌曲
  - 回复文案

### 第 5 层：播放执行层

建议直接使用 `mpv`：

- 后端给 `mpv` 下发播放命令
- 前端只展示：
  - 正在播放什么
  - 是否播放成功
  - 用户反馈操作

这套分层的好处是：

- UI 层不容易变乱
- 后端可以独立演进
- 以后你们想换记忆实现或换推荐器也不会伤到桌宠壳

---

## 当前项目怎么分工最合理

### `DyberPet` 在这次项目里的定位

它不是“参考项目”而是你们的**前端底座**。

你真正会改到的区域主要是：

- 入口与主窗体：`run_DyberPet.py`
- 桌宠主逻辑：`DyberPet/DyberPet.py`
- 气泡逻辑：`DyberPet/bubbleManager.py`
- 角色资源：`res/role/...`
- 图标资源：`res/icons/...`

### `ActivityWatch` 在这次项目里的定位

它不是你要改 UI 的项目，而是一个**外部环境数据源**。

它给你们提供：

- 当前活跃窗口
- AFK 状态
- 浏览器/应用使用情况

前端不应直接深改 `ActivityWatch`，而应由后端去读取它的数据，再把“已处理过的状态结果”发给前端。

### 前后端边界建议

前端不要自己做：

- 环境规则判断
- Memory 检索
- LLM 决策
- 歌曲推荐逻辑
- `mpv` 播放决策
- 原始环境数据清洗

前端只接收：

- `current_state`
- `state_reason`
- `recommended_track`
- `player_status`
- `assistant_reply`
- `bubble_text`

这样最稳。

---

## 基于现有代码，你该优先关注的文件

下面这些文件，是你前端开发最值得先读懂的部分。

### 1. 入口和整体装配

`DyberPet-main/run_DyberPet.py`

这里负责：

- 创建应用
- 初始化主桌宠
- 初始化通知系统
- 初始化控制面板和 Dashboard
- 连接各类 signal

你后面如果要加“联调入口”或“定时拉取后端状态”，这里是候选挂载点之一。

### 2. 桌宠主界面与交互

`DyberPet-main/DyberPet/DyberPet.py`

这是最关键的前端主文件，里面已经有：

- 透明桌宠窗体
- 鼠标点击/拖拽
- 右键菜单
- 状态栏
- 托盘
- 气泡注册
- 切换角色
- 切换动作

如果你要做：

- 新角色加载
- 状态切换表现
- 新增快捷交互
- 推荐结果提示

大概率都要改这里。

### 3. 气泡逻辑

`DyberPet-main/DyberPet/bubbleManager.py`

这里负责：

- 气泡配置读取
- 气泡触发
- 气泡文案替换

你后续可以把它扩展成：

- `focus_recommend`
- `tired_recommend`
- `music_started`
- `music_feedback_like`

这类新的业务气泡类型。

### 4. 角色素材规范

`DyberPet-main/docs/art_dev.md`

这份文档很关键，因为它说明了：

- 怎么新增角色
- 怎么组织动作帧
- `pet_conf.json` 和 `act_conf.json` 怎么配
- 怎么配气泡和角色信息

如果你要在黑客松里快速做“自己的桌宠形象”，这份文档是最直接的开发说明书。

---

## 黑客松前端目标版本

你这次前端最好的交付物，不是“功能最多”，而是“评委一眼看懂 + 可以稳定演示”。

### 必须有的前端能力

1. 自己的桌宠形象
2. 至少 4 个状态表现
3. 一种用户输入方式
4. 一种推荐反馈展示
5. 一种播放成功反馈

### 可以放弃的前端能力

- 复杂设置页
- 完整录音工作流
- 重型动画系统
- 多页面后台管理
- 大量二级交互

---

## 建议你负责的状态设计

结合 `product_design.md`，我建议前端只实现下面 5 个状态：

```text
idle
focus
tired
frustrated
sad
```

### 对应表现建议

| 状态 | 视觉表现 | 气泡文案方向 | 是否触发推荐 |
|---|---|---|---|
| idle | 安静待机 | 陪伴感 | 否 |
| focus | 微呼吸 / 戴耳机 | 不打扰、进入心流 | 是 |
| tired | 打哈欠 / 眼神疲惫 | 放松、轻安抚 | 是 |
| frustrated | 皱眉 / 小幅抖动 | 降躁、安抚 | 是 |
| sad | 缩起 / 低饱和 | 陪伴、理解 | 是 |

说明：

- 前端不需要知道状态怎么判断出来
- 前端只需要拿到状态字符串和一段提示词
- 然后做正确的视觉切换

---

## 推荐前端 API 契约

哪怕后端还没写完，你也应该先和后端同学把接口假定好。

### 1. 获取当前桌宠状态

```json
{
  "current_state": "tired",
  "state_reason": "late_night_coding",
  "bubble_text": "你看起来有点累，我放点轻一点的。",
  "pet_action": "tired",
  "can_recommend": true,
  "player_status": "idle"
}
```

### 2. 用户发起一次请求

用户输入：

```json
{
  "text": "我有点烦，来点适合现在的歌",
  "input_source": "text"
}
```

或由录音识别后提交：

```json
{
  "text": "我有点烦，来点适合现在的歌",
  "input_source": "faster_whisper"
}
```

后端返回：

```json
{
  "assistant_reply": "我感觉你现在有点紧绷，先给你放一点舒缓但不太丧的。",
  "current_state": "frustrated",
  "bubble_text": "先帮你把状态缓下来。",
  "recommended_track": {
    "title": "Night Walk",
    "artist": "Sample Artist",
    "source": "mpv"
  },
  "player_status": "playing",
  "play_action": "play"
}
```

### 3. 用户反馈

```json
{
  "track_id": "xxx",
  "feedback": "like"
}
```

你前端只要围绕这 3 类交互组织就够了。

---

## 你这次前端的最小页面与入口

严格来说，这个项目不需要“网页式多页面”，你只需要 4 个前端可见入口：

### 入口 1：桌宠本体

展示：

- 当前角色
- 当前状态动画
- 气泡反馈

### 入口 2：快捷输入

本次建议固定为：

- 一个小悬浮输入窗
- 一个文本输入框
- 一个录音按钮
- 一段识别结果预览

推荐方案：**文本输入为主，录音走 `faster-whisper` 转文本**

原因：

- 最快
- 不破坏 `DyberPet` 主体结构太多
- 最适合黑客松演示
- 输入主链路始终统一成 `text`
- 便于后端统一接收和调试

推荐交互顺序：

1. 用户点击输入入口
2. 可直接输入文本
3. 或点击录音按钮开始录音
4. 录音结束后由 `faster-whisper` 输出文本
5. 文本先展示给用户确认
6. 再发送给后端

### 入口 3：推荐反馈卡片

最简单做法：

- 展示歌名
- 展示一句推荐理由
- 提供 `喜欢 / 不喜欢 / 换一首`

### 入口 4：播放状态反馈

例如：

- “已开始播放”
- “已暂停”
- “已切换”
- “已交给 `mpv` 播放”

这层反馈一定要有，不然评委会看不清系统到底做没做事。

---

## 具体开发流程

下面是最适合你当前职责的实际开发顺序。

## Phase 0：当天先完成的判断

目标：

- 确认前端路线
- 确认桌宠角色路线
- 确认联调方式

你要做的事：

1. 和队友统一决策：本次前端基于 `DyberPet` 改，不新起 Electron
2. 定 1 个桌宠角色设定
3. 定 5 个状态
4. 定前后端最小接口字段
5. 确认语音识别方案固定为 `faster-whisper`
6. 确认播放执行层固定为 `mpv`

产出：

- 一页共识
- 一张状态表
- 一份假接口 JSON

---

## Phase 1：先做“面子工程”

这是你最优先的工作。

### 任务 1：替换角色资源

在 `res/role/` 下新建你们自己的角色目录，按 `art_dev.md` 规范准备：

- `action/`
- `pet_conf.json`
- `act_conf.json`
- 可选 `info/`
- 可选 `note/`

### 任务 2：先只做 4-5 组动作

建议动作最少集合：

- `default`
- `focus`
- `tired`
- `frustrated`
- `sad`
- `drag`
- `fall`

如果时间更紧，`focus/tired/frustrated/sad` 也可以先退化成：

- 几张静态图切换
- 而不是完整帧动画

### 任务 3：统一品牌资源

替换：

- 应用 icon
- 托盘 icon
- 角色头像
- 角色名字
- 默认气泡文案

产出：

- 一个“看起来已经像你们产品”的前端版本

---

## Phase 2：先做假数据状态切换

这一步非常关键。

不要一开始就等后端。

你应该先在前端里做一个**假状态切换开关**，比如：

- 按键 `1` -> `idle`
- 按键 `2` -> `focus`
- 按键 `3` -> `tired`
- 按键 `4` -> `frustrated`
- 按键 `5` -> `sad`

### 这一步的目标

- 验证角色资源是否完整
- 验证状态切换是否流畅
- 验证每个状态的气泡是否正确
- 验证展示是否足够打动人

### 前端此时需要完成

- 状态到动作的映射
- 状态到气泡的映射
- 状态到推荐卡文案的映射

产出：

- 一个无需后端也可演示的“桌宠状态版 Demo”

这一步做完，你们的项目就已经有了展示价值。

---

## Phase 3：做输入入口

### 推荐方案

优先做一个小输入框，并在同一个输入窗里挂上录音按钮。

你可以这样实现：

- 点击桌宠或菜单按钮
- 弹出一个迷你输入窗
- 用户输入一句话
- 或点击录音按钮采集音频
- 录音结果先交给 `faster-whisper`
- 把识别出的文本回填到输入框
- 用户确认后再提交
- 前端把文本发给后端
- 等待返回后更新桌宠状态和推荐反馈

### 前端任务

- 输入框 UI
- 录音按钮 UI
- 识别中文本提示
- 识别结果预览
- 加载态
- 错误态
- 返回结果展示

产出：

- `用户打字/录音 -> faster-whisper 产出文本 -> 前端提交 -> 桌宠回应`

---

## Phase 4：接后端状态接口

这一步开始联调。

### 联调顺序建议

1. 先接“只返回状态”的接口
2. 再接“文本输入 + Agent 回复”的接口
3. 再接“返回推荐歌曲”的接口
4. 最后接“反馈更新 Memory”的接口

### 前端只需要保证

- 能轮询或主动请求状态
- 能收到返回结果
- 能稳定更新桌宠表现

### 建议的联调方式

最简单可行方案：

- 后端提供本地 HTTP API
- 前端定时请求或在用户输入后请求
- 录音识别结果统一转成文本后再提交

不建议现在做：

- WebSocket
- 复杂事件总线
- 多进程复杂通信

---

## Phase 5：接音乐反馈展示

注意，这一步建议由后端控制 `mpv`，前端只负责反馈展示和反馈按钮。

你更重要的是把“结果”展示出来。

### 你要负责展示的最小信息

- 推荐歌名
- 推荐理由
- 当前播放状态
- 用户反馈按钮
- 是否已成功交给 `mpv`

### 用户反馈按钮最少做 3 个

- 喜欢
- 不喜欢
- 换一首

这会让产品一下子“像真的”。

### `mpv` 的推荐接法

建议后端做：

- 启动 `mpv`
- 发送播放 / 暂停 / 切歌命令
- 返回：
  - 当前播放歌曲
  - 播放是否成功
  - 播放器状态

前端做：

- 接收 `player_status`
- 展示“已播放 / 播放失败 / 已切歌”

---

## 48 小时前端排期

下面这版排期，是按黑客松强约束来写的。

## Day 1 上午

目标：跑通项目、看懂前端底座

你做：

- 跑通 `DyberPet`
- 熟悉 `DyberPet.py`
- 确认角色素材目录结构
- 和队友确认状态字段
- 和后端确认 `faster-whisper` 与 `mpv` 的接口边界

交付：

- 能本地跑
- 有角色目录模板

## Day 1 下午

目标：完成视觉换皮

你做：

- 新角色目录
- 主立绘与状态图
- icon 与气泡文案替换

交付：

- 第一版 EchoPet 外观

## Day 1 晚上

目标：完成假状态切换

你做：

- 加状态映射
- 加手动切换
- 验证 5 个状态表现
- 预留输入窗位置和推荐卡位置

交付：

- 可单独演示的前端 Demo

## Day 2 上午

目标：完成输入入口与联调壳层

你做：

- 小输入框
- 录音按钮
- `faster-whisper` 识别回填文本
- 请求后端的接口壳
- 加载态/错误态

交付：

- 用户输入可驱动前端变化

## Day 2 下午

目标：接推荐结果与播放反馈

你做：

- 推荐卡片
- 播放状态提示
- 反馈按钮
- `mpv` 播放状态联调

交付：

- “桌宠理解你并播歌”的完整前端链路

## Day 2 晚上

目标：做演示优化

你做：

- 优化文案
- 优化状态切换节奏
- 修掉显示问题
- 演示模式开关

交付：

- 可答辩版本

---

## 前端联调检查清单

在联调前，你应该把下面这些先准备好。

### 资源层

- 角色目录可加载
- 所有状态图路径正确
- 缺图时有兜底图

### 交互层

- 输入框能打开和关闭
- 录音按钮能开始和停止
- 识别结果能正确回填文本
- 请求中有 loading
- 失败后有错误提示

### 状态层

- 任意状态切换都不会崩
- 无返回结果时回退到 `idle`
- 气泡不会无限叠加
- 识别失败时不会卡死输入流程

### 演示层

- 桌宠启动位置稳定
- 文案不过长
- 切换状态有明显差异

---

## 你最该主动推动团队确认的接口细节

因为你是前端负责人，这几件事一定要尽早和后端说清楚：

1. 状态枚举是否固定
2. 每个状态对应的气泡文案由谁生成
3. 前端是否只展示字符串，还是后端返回动作名
4. 推荐歌曲字段是否固定
5. 用户反馈按钮对应的接口字段是什么
6. `faster-whisper` 放在前端侧适配层还是后端服务侧
7. `mpv` 的状态回传格式是什么

如果这些不先统一，黑客松联调会很痛苦。

---

## 明确不建议做的事

下面这些看起来很酷，但黑客松里容易拖垮进度。

- 重新做一个全新桌宠前端
- 同时做语音、动画、播放器控制、复杂记忆 UI
- 给每个状态做很多复杂动作
- 把所有逻辑都塞进前端
- 在最后一天才开始联调
- 让前端直接承担 `mpv` 播放控制与复杂记忆编排

---

## 这次前端成功的判断标准

如果最终能做到下面这 5 条，你这部分就已经非常成功：

1. 桌面上出现一个明显属于你们自己的桌宠
2. 它能根据状态切换不同表现
3. 用户能说一句话或输入一句话
4. 桌宠会给出“理解你”的反馈
5. 评委能看到它真的推荐并播放了音乐

这就足够支撑一个高完成度黑客松演示。

---

## 我对你的最务实建议

作为前端负责人，你接下来最该做的不是继续找更多开源仓库，而是马上推进这 4 件事：

1. 先在 `DyberPet` 里把你们自己的角色跑起来
2. 先把 5 个状态做成假数据可切换
3. 先做一个带录音按钮的最小输入框
4. 先把 `faster-whisper -> text` 路径打通
5. 先和后端统一接口 JSON
6. 先确定 `mpv` 的播放状态回传字段

一旦这 6 步完成，你们整个项目就会进入“稳态推进”。

---

## 如果让我继续帮你

我下一步最适合继续写的是下面两份之一：

1. `前端任务拆解清单.md`
   - 精确到你每天改哪些文件
   - 每个模块怎么验收

2. `DyberPet 前端改造点位说明.md`
   - 直接按文件告诉你哪些地方适合挂状态机、输入框、推荐反馈

如果你愿意，我建议我下一条直接继续给你写：

**`DyberPet 前端改造点位说明.md`**

这个会更像真正能开工的开发说明。
