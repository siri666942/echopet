# EchoPet 前端开发进度

更新时间：2026-05-30

## 1. 当前结论

当前前端开发已经按 `frontend_integration.md` 对齐，职责边界如下：

- 前端基座：`DyberPet`
- 录音：前端本地录音
- 转写：调用主后端 `POST /api/transcribe`
- 分析：调用主后端 `POST /api/analyze`
- 播放控制：后端负责控制 `mpv`
- 播放展示：前端轮询 `GET /api/player/status`
- 反馈：前端调用 `POST /api/feedback`

一句话：

**前端不直接控制 `mpv`，只负责输入、状态展示、结果展示和反馈。**

---

## 2. 已完成进度

### 已完成的基础能力

- 已完成 `DyberPet` 菜单入口接入，可打开 EchoPet 输入面板
- 已将右键菜单收敛为黑客松 Demo 模式，主层只保留 EchoPet 主入口和退出
- 已完成最小输入面板，可支持文本输入
- 已完成前端本地录音能力
- 已完成录音结束后调用 `/api/transcribe`
- 已完成 transcript 自动回填文本框
- 已完成文本提交到 `/api/analyze`
- 已完成根据分析结果切换桌宠状态和气泡文案
- 已完成推荐歌曲信息展示
- 已完成用户反馈按钮接线
- 已完成在 analyze 返回后轮询 `/api/player/status`

### 已完成的工程化内容

- 已抽出 `frontend/` 适配层目录
- 已拆出 `agent_client.py`
- 已拆出 `whisper_adapter.py`
- 已拆出 `audio_recorder.py`
- 已拆出 `state_mapper.py`
- 已拆出 `input_panel.py`
- 已拆出 `player_status_poller.py`
- 已将原版桌宠的开发态入口收进 `Debug Tools`

---

## 3. 当前前端实现方式

当前实现方式不是重写一个新桌宠，而是在 `DyberPet` 上增加一层适配层。

### 现在的真实链路

```text
用户点击输入面板录音
→ 前端本地录音
→ POST /api/transcribe
→ transcript 回填输入框
→ 用户确认文本并点击提交
→ GET /api/context
→ POST /api/analyze
→ 前端切换桌宠状态 / 气泡 / 推荐信息
→ 前端轮询 GET /api/player/status
→ 用户点击反馈
→ POST /api/feedback
```

### 当前目录分工

```text
frontend/
├─ agent_client.py
│  └─ 后端 HTTP 接口封装
├─ audio_recorder.py
│  └─ 本地录音
├─ whisper_adapter.py
│  └─ /api/transcribe 适配
├─ state_mapper.py
│  └─ 后端字段映射到前端状态
├─ input_panel.py
│  └─ 输入窗、录音按钮、反馈按钮
└─ player_status_poller.py
   └─ 轮询 /api/player/status
```

### 前后端边界

- 前端负责：
  - 录音
  - 文本输入
  - 调接口
  - 桌宠状态切换
  - 气泡展示
  - 推荐信息展示
  - 播放状态展示
  - 反馈提交

- 后端负责：
  - 音频转文字
  - 环境上下文
  - 情绪分析
  - 推荐结果
  - `mpv` 播放控制
  - 播放器真实状态

### 当前入口结构

- 右键菜单主层：
  - `Open EchoPet`
  - `Debug Tools`
  - `Exit`
- 右键菜单顶部信息区：
  - `宠物状态`
  - `当前推荐`
  - `播放状态`
- 输入面板主层：
  - 文本输入
  - 录音按钮
  - 提交按钮
  - 当前推荐和反馈
- 开发态功能：
  - 快速状态切换
  - 原版 `Dashboard/System/动作/伙伴/切角色`
  - 已收进 `Debug Tools` 或输入面板折叠区

### 当前状态区语义

- `宠物状态`
  - 用来显示 EchoPet 当前在做什么
  - 例如：`待命中`、`正在听你说话`、`正在转写语音`、`正在帮你找歌`、`正在陪你听歌`
- `当前推荐`
  - 用来显示当前推荐的歌曲标题和歌手
- `播放状态`
  - 用来显示 `未播放 / 正在准备歌曲 / 正在播放 / 已暂停 / 播放异常`

---

## 4. 当前未完成项

- 角色资源还没有完整替换成你们自己的正式美术
- `focus / tired / frustrated / sad` 还没有全部绑定到正式状态素材
- 输入面板视觉还是功能型原型，尚未做产品化美化
- 推荐信息展示还比较简陋
- 播放状态展示还只是文字提示
- 错误提示还可以继续细化

---

## 5. 前端美化步骤

这是接下来非常建议推进的一块。

### Step 1：统一输入面板视觉

- 调整输入面板宽高、留白、字体层级
- 统一主按钮、次按钮、危险提示颜色
- 让输入面板更像正式产品而不是调试面板

### Step 2：把推荐结果做成卡片

- 把当前歌曲名、歌手、推荐理由整理成更明确的卡片
- 增加状态标签，比如 `专注 / 疲惫 / 烦躁`
- 让评委一眼看到系统“理解了什么、推荐了什么”

### Step 3：优化桌宠气泡文案

- 把当前气泡从纯功能提示改成更像 EchoPet 角色说话
- 让不同状态的文案更有区分度

### Step 4：补正式状态素材

- 给 `idle / focus / tired / frustrated / sad` 补齐视觉资源
- 让状态切换不只靠文案，而是靠角色表达本身

### Step 5：补播放器展示样式

- 把播放中、加载中、暂停、失败做成更明显的轻量提示
- 避免只靠一行状态文字

---

## 6. 接下来建议的开发顺序

### 第一阶段：把主链路彻底稳定

1. 稳定 `/api/transcribe` 回填
2. 稳定 `/api/analyze` 结果展示
3. 稳定 `/api/player/status` 轮询展示
4. 稳定 `/api/feedback` 提交

### 第二阶段：美化前端

1. 美化输入面板
2. 美化推荐展示
3. 美化状态气泡
4. 替换正式角色素材

### 第三阶段：演示优化

1. 缩短无关调试提示
2. 优化状态切换节奏
3. 优化演示文案
4. 准备答辩演示脚本

---

## 7. 对 mpv 的最终判断

根据当前 `frontend_integration.md` 和 `api_docs.md`：

- 前端**不需要实现 `mpv` 播放控制**
- 前端**也不应该直接启动或控制 `mpv`**
- 前端只需要：
  - 接收 `analyze` 的结果
  - 轮询 `/api/player/status`
  - 展示当前播放状态

所以：

**`mpv` 的控制权在后端，不在前端。**

---

## 8. 当前最值得继续做的事

如果继续推进，我建议优先级如下：

1. 美化 `input_panel.py`
2. 增加推荐卡片 UI
3. 给桌宠补正式状态资源
4. 优化录音与转写错误提示
5. 优化播放状态展示
