"""EchoPet 后端 Python 包。

这个文件叫 `__init__.py`，它的作用是告诉 Python：

    backend/ 这个目录是一个可以被 import 的包。

所以其他地方才能写：

    from backend.config import settings
    from backend.api import analyze

这个文件本身不放业务逻辑，只做包声明。
"""
